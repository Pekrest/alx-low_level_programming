#include "main.h"
/**
 * add - a function that adds two integers and returns the result.
 *
 * @a: integer to check
 * @b: integer to check
 *
 * Return: s (a && b sum)
 *
 */
int add(int a, int b)
{
	int s;

	s = a + b;
	return (s);
}
